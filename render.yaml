services:
  - type: web
    name: bsm-greeks-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 5000
      - key: FLASK_ENV
        value: production
